name = "line-crm-liff"
compatibility_date = "2024-12-01"
pages_build_output_dir = "dist"

# Pages deployには--envがないため、package.jsonのdeploy:productionがこのprojectを明示する。
[env.production]
name = "line-crm-liff-prod"
